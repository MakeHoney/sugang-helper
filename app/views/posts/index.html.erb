<head>
  <link href="https://fonts.googleapis.com/css?family=Monoton|Orbitron|Stylish" rel="stylesheet">
 <style>
   #font1{
     font-family: 'Monoton', cursive;
     text-align: center;
     font-size: 80px;
   }
   h1{
     color:white;
   }
    #font2{
     font-family: 'Orbitron', sans-serif;
     text-align: center;
     font-size: 25px;
   }
    h2{
     color:white;
   }
    /*#font3{*/
    /* font-family: 'Stylish', sans-serif;*/
    /* text-align: center;*/
    /* font-size: 50px;*/
    /*}*/
    /*h3{*/
    /* color:white;*/
    /*}*/
 </style>
</head>


<body class = "test1" id="test2">
   <div class="content" >
     <br><br><br><br>
     <h1 id="font1">
       AJOU <br> HELPER
     </h1>
     <h2 id="font2">
       ajou university
     </h2>
<body class = "test5" id = "test6">
</div>
<p id="notice"><%= notice %></p>

<div class="container">
  
  <h1><strong>질문게시판</strong></h1>
  

  <div id="myBtnContainer">
    <button class="btn active" onclick="filterSelection('all')"> 전체보기</button>
    <button class="btn" onclick="filterSelection('showall')"> 전체보기</button>
    <button class="btn" onclick="filterSelection('software')"> 소프트웨어학과</button>
    <button class="btn" onclick="filterSelection('cyber_security')"> 사이버보안학과</button>
  </div>
</div>

<div class="container">

  <div class="filterDiv showall">
    <h3>전체 글</h3>
    <table>
      <thead>
        <tr>
          <th>제목</th>
          <th>학과</th>
          <th>작성자</th>
          <th>작성시간</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @posts.each do |post| %>
          <tr>
            <% if user_signed_in? %>
              <td class="col-md-6 col-xs-6"><%= link_to post.title, post %> [<%= post.comments.count %>]</td>
            <% else %>
              <td class="col-md-6 col-xs-6"><%= post.title %> [<%= post.comments.count %>]</td>
            <% end %>
            <td><%= Category.find(post.category_id).name %></td>
            
            <% if Student.exists?(stu_email: post.user.email) %>
              <td><%= Student.find_by(stu_email: post.user.email).stu_name %></td>
            <% elsif post.user.email.eql?("admin@ajou.ac.kr") %>
              <td>관리자</td>
            <% else %>
              <td><%= post.user.email %></td>
            <% end %>
            
            <td><%= time_ago_in_words(post.created_at) %> ago</td>
            <td>
              <% if user_signed_in? %>
                <%= link_to icon('edit'), edit_post_path(post) if post.updatable_by?(current_user)%>&nbsp;
                <%= link_to icon('trash'), post, method: :delete, data: { confirm: '정말 삭제하시겠어요?' } if post.deletable_by?(current_user) %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>

  <div class="filterDiv software">
    <h3>소프트웨어학과</h3>
    <table>
      <thead>
        <tr>
          <th>제목</th>
          <th>학과</th>
          <th>작성자</th>
          <th>작성시간</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @postsSoft.each do |post| %>
          <tr>
            <% if user_signed_in? %>
              <td class="col-md-6 col-xs-6"><%= link_to post.title, post %> [<%= post.comments.count %>]</td>
            <% else %>
              <td class="col-md-6 col-xs-6"><%= post.title %> [<%= post.comments.count %>]</td>
            <% end %>
            <td><%= Category.find(post.category_id).name %></td>
            
            <% if Student.exists?(stu_email: post.user.email) %>
              <td><%= Student.find_by(stu_email: post.user.email).stu_name %></td>
            <% elsif post.user.email.eql?("admin@ajou.ac.kr") %>
              <td>관리자</td>
            <% else %>
              <td><%= post.user.email %></td>
            <% end %>
            
            <td><%= time_ago_in_words(post.created_at) %> ago</td>
            <td>
              <% if user_signed_in? %>
                <%= link_to icon('edit'), edit_post_path(post) if post.updatable_by?(current_user)%>&nbsp;
                <%= link_to icon('trash'), post, method: :delete, data: { confirm: '정말 삭제하시겠어요?' } if post.deletable_by?(current_user) %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>

  <div class="filterDiv cyber_security">
    <h3>사이버보안학과</h3>
    <table>
      <thead>
        <tr>
          <th>제목</th>
          <th>학과</th>
          <th>작성자</th>
          <th>작성시간</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @postsCyber.each do |post| %>
          <tr>
            <% if user_signed_in? %>
              <td class="col-md-6 col-xs-6"><%= link_to post.title, post %> [<%= post.comments.count %>]</td>
            <% else %>
              <td class="col-md-6 col-xs-6"><%= post.title %> [<%= post.comments.count %>]</td>
            <% end %>
            <td><%= Category.find(post.category_id).name %></td>
            
            <% if Student.exists?(stu_email: post.user.email) %>
              <td><%= Student.find_by(stu_email: post.user.email).stu_name %></td>
            <% elsif post.user.email.eql?("admin@ajou.ac.kr") %>
              <td>관리자</td>
            <% else %>
              <td><%= post.user.email %></td>
            <% end %>
            
            <td><%= time_ago_in_words(post.created_at) %> ago</td>
            <td>
              <% if user_signed_in? %>
                <%= link_to icon('edit'), edit_post_path(post) if post.updatable_by?(current_user)%>&nbsp;
                <%= link_to icon('trash'), post, method: :delete, data: { confirm: '정말 삭제하시겠어요?' } if post.deletable_by?(current_user) %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>

<div class="container">
  <div style="float: right;">

  <%= link_to '글쓰기', new_post_path, class: 'btn btn-default' %>
    
  </div>
</div>

<br>
</body>